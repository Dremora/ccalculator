package ccalculator.functions;

/**
 * Function abs.
 * 
 * Returns absolute value of a number.
 */
public class Abs {
	public static double abs(double a)
	{
		return (a > 0) ? a : -a;
	}
}
